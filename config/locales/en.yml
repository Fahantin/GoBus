# Files in the config/locales directory are used for internationalization
# and are automatically loaded by Rails. If you want to use locales other
# than English, add the necessary files in this directory.
#
# To use the locales, use `I18n.t`:
#
#     I18n.t 'hello'
#
# In views, this is aliased to just `t`:
#
#     <%= t('hello') %>
#
# To use a different locale, set it with `I18n.locale`:
#
#     I18n.locale = :es
#
# This would use the information in config/locales/es.yml.
#
# To learn more, please read the Rails Internationalization guide
# available at http://guides.rubyonrails.org/i18n.html.

en:
  activerecord:
    errors:
      models:
        usuario:
          attributes:	
            senha:
              blank: (Obrigatório)
              too_short: (Mínimo 8 Caracteres)
              too_long: (Máximo 10 Caracteres)
            nome:
              blank: (Obrigatório)
              too_short: (Mínimo 2 Caracteres)
              too_long: (Máximo 80 Caracteres)
            email:
              blank: (Obrigatório)
              too_short: (Mínimo 5 Caracteres)
              too_long: (Máximo 100 Caracteres)
            login:
              blank: (Obrigatório)
              too_short: (Mínimo 2 Caracteres)
              too_long: (Máximo 25 Caracteres)

        linha:
          attributes:	
            numero:
              blank: (Obrigatório)
            descricao:
              blank: (Obrigatório)
              too_short: (Mínimo 1 Caracteres)
              too_long: (Máximo 200 Caracteres)

        onibus:
          attributes:	
            placa:
              blank: (Obrigatório)
              wrong_length: (Necessário 7 Caracteres)
            linha_id:
              blank: (Obrigatório)